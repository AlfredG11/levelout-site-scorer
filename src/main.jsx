import React from 'react'
import ReactDOM from 'react-dom/client'
import LevelOutSiteScorer from './LevelOutSiteScorer'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <LevelOutSiteScorer />
  </React.StrictMode>
)
